# Single source of truth for the llama.cpp headers and runtime C ABI.
# Any bump must update both values together: the public header release tag and
# the protocol written to downloaded runtime manifests.
set(LASTUDIO_LLAMA_CPP_REF "b10036")
set(LASTUDIO_LLAMA_PROTOCOL_VERSION "llama-c-api-b10036")
